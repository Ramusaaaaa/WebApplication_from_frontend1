export class Product {
    constructor(

            id: number,
            title: string,
            description: string,
            image: string,
            price: number

            // eslint-disable-next-line @typescript-eslint/no-empty-function
    ) {
    }
}